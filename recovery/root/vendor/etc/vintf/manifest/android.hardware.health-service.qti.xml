<!--
    Health AIDL HAL manifest fragment — myron
    Được merge bởi libvintf vào tổng device manifest.
    servicemanager dùng manifest tổng này để allow AServiceManager_addService()
-->
<manifest version="2.0" type="device">
    <hal format="aidl">
        <name>android.hardware.health</name>
        <version>3</version>
        <fqname>IHealth/default</fqname>
    </hal>
</manifest>
