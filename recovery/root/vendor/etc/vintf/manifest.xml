<!--
    Vendor VINTF manifest — myron (Redmi K90 Pro Max / SM8850 canoe)
    Platform corrected from sun to canoe for WPMCNXM compatibility.
    NOTE: android.hardware.keymaster@4.1 entry added FOR OFOX KEYMASTER DETECTION ONLY.
    OFox R11.x/R12.x Keymaster_Ver() searches for this HIDL entry to determine decrypt path.
    The actual decrypt uses KeyMint AIDL (version 3) — this entry is only for OFox parser.
    FIX: version 9.0 for Android 16 / libvintf@9.0 compatibility
    FIX: <n>→<name> per libvintf XML schema
-->
<manifest version="8.0" type="device" target-level="202504">
    <!-- OFox keymaster version detection (HIDL legacy entry required by OFox parser) -->
    <hal format="hidl">
        <name>android.hardware.keymaster</name>
        <transport>hwbinder</transport>
        <version>4.1</version>
        <interface>
            <name>IKeymasterDevice</name>
            <instance>default</instance>
        </interface>
    </hal>
    <!-- Actual KeyMint AIDL (used for real decrypt) -->
    <hal format="aidl">
        <name>android.hardware.security.keymint</name>
        <version>3</version>
        <fqname>IKeyMintDevice/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.security.keymint</name>
        <version>3</version>
        <fqname>IRemotelyProvisionedComponent/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.gatekeeper</name>
        <fqname>IGatekeeper/default</fqname>
    </hal>
    <!-- Weaver (小米特有，用于指纹安全存储) -->
    <hal format="aidl">
        <name>android.hardware.weaver</name>
        <fqname>IWeaver/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.secure_element</name>
        <fqname>ISecureElement/eSE1</fqname>
    </hal>
    <sepolicy>
        <version>202504</version>
    </sepolicy>
</manifest>