<!--
    Vendor VINTF manifest — myron (POCO F8 Ultra / SM8850 sun)
    Confirmed from: adb shell cat /vendor/etc/vintf/manifest.xml (stock ROM)

    NOTE: android.hardware.keymaster@4.1 entry added FOR OFOX KEYMASTER DETECTION ONLY.
    OFox R11.x Keymaster_Ver() searches for this HIDL entry to determine decrypt path.
    Without it, OFox crashes in a loop before falling back (causing 5s UI lag per action).
    The actual decrypt uses KeyMint AIDL (version 3) — this entry is only for OFox parser.
-->
<manifest version="9.0" type="device">
    <!-- OFox keymaster version detection (HIDL legacy entry required by OFox parser) -->
    <hal format="hidl">
        <name>android.hardware.keymaster</name>
        <transport>hwbinder</transport>
        <version>4.1</version>
        <interface>
            <name>IKeymasterDevice</name>
            <instance>default</instance>
        </interface>
    </hal>
    <!-- Actual KeyMint AIDL (used for real decrypt) -->
    <hal format="aidl">
        <n>android.hardware.security.keymint</n>
        <version>3</version>
        <fqname>IKeyMintDevice/default</fqname>
    </hal>
    <hal format="aidl">
        <n>android.hardware.security.keymint</n>
        <version>3</version>
        <fqname>IRemotelyProvisionedComponent/default</fqname>
    </hal>
    <hal format="aidl">
        <n>android.hardware.gatekeeper</n>
        <fqname>IGatekeeper/default</fqname>
    </hal>
    <hal format="aidl">
        <n>android.hardware.secure_element</n>
        <fqname>ISecureElement/eSE1</fqname>
    </hal>
</manifest>
